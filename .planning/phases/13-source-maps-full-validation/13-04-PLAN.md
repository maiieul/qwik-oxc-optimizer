---
phase: 13-source-maps-full-validation
plan: 04
type: execute
wave: 4
depends_on: ["03"]
files_modified:
  - crates/qwik-optimizer-oxc/tests/spec_tests.rs
  - crates/qwik-optimizer-oxc/src/lib.rs
  - crates/qwik-optimizer-oxc/src/code_move.rs
  - crates/qwik-optimizer-oxc/src/hash.rs
autonomous: true

must_haves:
  truths:
    - "All 162 spec files transform without errors (zero transform failures)"
    - "Module count matches for all 162 specs (162/162)"
    - "Segment metadata (ctxName, ctxKind, captures) matches spec expectations for all entry point modules"
    - "Diagnostics produced by the optimizer match spec-expected diagnostics where specified"
    - "All spec validation runs with source_maps=true (default)"
  artifacts:
    - path: "crates/qwik-optimizer-oxc/tests/spec_tests.rs"
      provides: "test_full_spec_validation asserting 162/162 module count match, metadata match, and diagnostic match"
    - path: "crates/qwik-optimizer-oxc/src/lib.rs"
      provides: "Final edge case fixes for preserve_filenames, explicit_extensions, multi-input"
  key_links:
    - from: "crates/qwik-optimizer-oxc/tests/spec_tests.rs"
      to: "qwik_optimizer_oxc::transform_modules"
      via: "Full validation test calling transform_modules for each of 162 specs with assertions"
      pattern: "transform_modules.*assert"
    - from: "crates/qwik-optimizer-oxc/tests/spec_tests.rs"
      to: "spec_parser::load_all_specs"
      via: "Loading and validating all 162 spec files"
      pattern: "load_all_specs.*162"
---

<objective>
Fix all remaining edge case failures and build the comprehensive 162/162 validation test with metadata and diagnostic assertions.

Purpose: Requirements TEST-02 and TEST-03 -- the optimizer must pass ALL 162 behavioral spec tests. After Plans 02-03 fixed the high-impact categories, this plan addresses the remaining ~15 edge cases (preserve_filenames, explicit_extensions, multi-input, diagnostics-only specs) and builds the final asserting validation test.

Output: 162/162 specs pass; comprehensive test_full_spec_validation exists and asserts correctness.
</objective>

<execution_context>
@/Users/jackshelton/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jackshelton/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-source-maps-full-validation/13-02-SUMMARY.md
@.planning/phases/13-source-maps-full-validation/13-03-SUMMARY.md
@crates/qwik-optimizer-oxc/tests/spec_tests.rs
@crates/qwik-optimizer-oxc/tests/spec_parser.rs
@crates/qwik-optimizer-oxc/src/lib.rs
@crates/qwik-optimizer-oxc/src/code_move.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix remaining edge case failures to reach 162/162 module count match</name>
  <files>crates/qwik-optimizer-oxc/src/lib.rs, crates/qwik-optimizer-oxc/src/code_move.rs, crates/qwik-optimizer-oxc/src/hash.rs</files>
  <action>
Run the diagnostic test to identify the remaining ~15 mismatched specs:
`cargo test --package qwik-optimizer-oxc test_diagnose_module_count_mismatches -- --nocapture`

Fix each remaining category. Based on the architecture, likely remaining issues are:

**preserve_filenames** (lib.rs segment path construction):
- When `preserve_filenames: true`, segment output paths should use the original function name in the filename rather than a hash-based canonical name
- Check how `segment_data_to_analysis()` constructs `canonical_filename`
- When preserve_filenames is true, the path format should be `{input_base}_{display_name}.{ext}` instead of `{input_base}_{display_name}_{hash}.{ext}`
- Fix the path construction in the segment module building loop (step 9 of transform_modules)

**explicit_extensions** (code_move.rs + lib.rs):
- When `explicit_extensions: true`, import paths in lazy imports should include the file extension (e.g., `./test.tsx_segment.js` instead of `./test.tsx_segment`)
- Check how lazy import paths are constructed in code_move.rs `build_segment_code_with_hoisted()` and in the main module's lazy import declarations
- Verify the segment module path includes the correct extension

**Multi-input / relative_paths** (lib.rs):
- Specs with `additional_inputs` provide multiple input modules
- Verify that `transform_modules` correctly processes all inputs in `config.input` and produces modules for each

**Diagnostics-only specs** (transform.rs + lib.rs):
- Specs expecting diagnostics (e.g., invalid_references, invalid_segment_expr) may produce different module counts because the transform aborts early or skips certain segments
- Check if diagnostic-producing specs expect fewer modules than the non-diagnostic case
- Ensure error diagnostics are generated (via qwik_transform.diagnostics()) and mapped to the output

For each fix:
1. Identify the spec(s) affected
2. Make the minimal code change
3. Run `cargo test --package qwik-optimizer-oxc test_all_specs_coverage_report -- --nocapture` to verify improvement
4. Repeat until module_count_match == 162

If any specs are genuinely impossible to match (e.g., hash differences causing path differences that affect module count), document them as known deviations with comments explaining why.
  </action>
  <verify>
`cargo test --package qwik-optimizer-oxc test_all_specs_coverage_report -- --nocapture` shows:
- Transform OK: 162
- Module count match: 162
- Module count mismatch: 0

`cargo test --package qwik-optimizer-oxc` passes all tests.
  </verify>
  <done>All 162 specs produce the correct number of output modules. Module count match is 162/162.</done>
</task>

<task type="auto">
  <name>Task 2: Build comprehensive validation test with metadata and diagnostic assertions</name>
  <files>crates/qwik-optimizer-oxc/tests/spec_tests.rs</files>
  <action>
Replace the existing `test_all_specs_coverage_report` with a comprehensive validation test `test_full_spec_validation` that asserts correctness (not just reports):

1. **Module count assertion** (hard assert): For each spec, assert actual module count == expected module count. If mismatch, print diagnostic details and fail.

2. **Segment metadata matching**: For each expected entry point module that has segment metadata (segment field in spec):
   - Find the matching actual module by looking for a module whose segment.ctx_name matches the expected segment's "ctxName" field
   - Assert: ctxKind matches (compare "event" vs "function" from spec JSON)
   - Assert: captures flag matches (true/false from spec JSON)
   - If spec has "captures" as true, verify the actual segment also has captures=true
   - Do NOT compare hashes (OXC and SWC produce different hashes)
   - Do NOT compare exact code (different codegen style)

3. **Diagnostic matching**: For specs with expected_diagnostics:
   - Assert the optimizer produces the same NUMBER of diagnostics
   - Assert diagnostic severity/category matches (Error, Warning, SourceError)
   - Do NOT compare exact message text (wording may differ between implementations)

4. **Report format** (printed with `--nocapture`):
   ```
   === Full Spec Validation ===
   Total: 162
   Module count match: 162/162
   Metadata match: N/162
   Diagnostics match: N/M (M specs have expected diagnostics)

   FAILURES:
   {spec_name}: {what failed - module count / metadata / diagnostics}
   ===========================
   ```

5. **Hard assertion**: `assert_eq!(failures.len(), 0, "...")` -- the test MUST pass with zero failures for the phase to be complete.

6. Keep the old `test_all_specs_coverage_report` as a separate quick-check test but update it to also assert module_count_match == specs.len().

7. Ensure source_maps=true for all spec runs (this is the default in TransformModulesOptions, so no special handling needed -- just verify it's not being overridden to false in build_options).
  </action>
  <verify>
`cargo test --package qwik-optimizer-oxc test_full_spec_validation -- --nocapture` passes and shows:
- Module count match: 162/162
- Metadata match: N/162 (should be high, ideally 162/162)
- Diagnostics match: N/M
- FAILURES: (none)

`cargo test --package qwik-optimizer-oxc` passes all tests.
  </verify>
  <done>Comprehensive validation test exists that verifies module counts, segment metadata, and diagnostics for all 162 specs. Test passes with zero failures. All specs run with source_maps=true.</done>
</task>

</tasks>

<verification>
1. `cargo test --package qwik-optimizer-oxc` -- all tests pass (unit + spec)
2. `cargo test --package qwik-optimizer-oxc test_full_spec_validation -- --nocapture` -- 162/162 specs validated, zero failures
3. Module count match: 162/162
4. Metadata match rate documented
5. Source maps active for all spec runs
6. No regressions in existing 146+ unit tests
</verification>

<success_criteria>
- All 162 spec files transform without errors
- Module count matches for all 162 specs (162/162)
- Segment metadata (ctxName, ctxKind, captures) verified for all entry point modules
- Diagnostics match spec expectations where defined
- Comprehensive test_full_spec_validation test exists and passes
- All spec runs use source_maps=true (verifying EMIT-02 + TEST-02 + TEST-03 together)
- All existing unit tests pass without regression
</success_criteria>

<output>
After completion, create `.planning/phases/13-source-maps-full-validation/13-04-SUMMARY.md`
</output>
