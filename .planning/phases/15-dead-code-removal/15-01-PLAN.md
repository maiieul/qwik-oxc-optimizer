---
phase: 15-dead-code-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/qwik-optimizer-oxc/src/errors.rs
  - crates/qwik-optimizer-oxc/src/words.rs
  - crates/qwik-optimizer-oxc/src/entry_strategy.rs
  - crates/qwik-optimizer-oxc/src/code_move.rs
  - crates/qwik-optimizer-oxc/src/emit.rs
  - crates/qwik-optimizer-oxc/src/import_rewrite.rs
  - crates/qwik-optimizer-oxc/src/props_destructuring.rs
  - crates/qwik-optimizer-oxc/src/collector.rs
autonomous: true

must_haves:
  truths:
    - "errors.rs has no #![allow(unused)] and no unused public functions"
    - "words.rs has no duplicate QWIK constants (both BUILDER_IO_QWIK and QWIK_CORE_ID eliminated -- unused)"
    - "entry_strategy.rs has no #![allow(unused)] and no unused functions"
    - "No unused functions exist in code_move.rs, emit.rs, import_rewrite.rs, or props_destructuring.rs"
    - "No unused variable warnings in collector.rs or props_destructuring.rs"
  artifacts:
    - path: "crates/qwik-optimizer-oxc/src/errors.rs"
      provides: "Only create_source_error remains (used by parse.rs)"
      contains: "pub(crate) fn create_source_error"
    - path: "crates/qwik-optimizer-oxc/src/words.rs"
      provides: "Only classify_ctx_kind and dollar_to_qrl_name remain (no constants needed)"
    - path: "crates/qwik-optimizer-oxc/src/entry_strategy.rs"
      provides: "Only should_inline remains (used by transform.rs + lib.rs)"
  key_links:
    - from: "crates/qwik-optimizer-oxc/src/parse.rs"
      to: "crates/qwik-optimizer-oxc/src/errors.rs"
      via: "errors::create_source_error"
      pattern: "errors::create_source_error"
---

<objective>
Delete unused functions, constants, and structs across 8 source files to eliminate compiler warnings.

Purpose: Satisfy DEAD-01 (errors.rs cleanup), DEAD-02 (words.rs consolidation), and half of success criterion 4 (no unused warnings) by removing genuinely dead code that has accumulated during v3.0 development.

Output: 8 modified source files with no unused function/constant/struct/variable warnings.
</objective>

<execution_context>
@/Users/jackshelton/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jackshelton/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete unused functions and constants from errors.rs, words.rs, and entry_strategy.rs</name>
  <files>
    crates/qwik-optimizer-oxc/src/errors.rs
    crates/qwik-optimizer-oxc/src/words.rs
    crates/qwik-optimizer-oxc/src/entry_strategy.rs
    crates/qwik-optimizer-oxc/src/collector.rs
  </files>
  <action>
    **errors.rs (DEAD-01):**
    1. Remove `#![allow(unused)]` on line 7
    2. Delete `create_error` function (lines 12-21) -- never called anywhere
    3. Delete `create_warning` function (lines 24-33) -- never called anywhere
    4. Keep `create_source_error` -- it IS used by parse.rs (lines 76, 85)

    **words.rs (DEAD-02):**
    1. Delete `BUILDER_IO_QWIK` constant (line 10) -- exact duplicate of `QWIK_CORE_ID` (line 13), both equal `"@qwik.dev/core"`. Neither is used in production code outside words.rs itself.
    2. Delete `QWIK_CORE_ID` constant (line 13) -- also unused outside words.rs (collector.rs has its own `is_qwik_core_import` method on CollectContext).
    3. Delete `KNOWN_DOLLAR_APIS` constant (lines 16-29) -- unused in production code (collector.rs identifies dollar APIs by checking if import names end with `$`, not via this list)
    4. Delete `is_known_dollar_api` function (lines 32-34) -- unused
    5. Delete `is_qwik_core_import` function (lines 87-89) -- unused (collector.rs has its own `CollectContext::is_qwik_core_import` method)
    6. Delete the words.rs `#[cfg(test)] mod tests` block that tests the deleted items (tests for `is_known_dollar_api`, `dollar_to_qrl_name`, `classify_ctx_kind`, `is_qwik_core_import`)
    7. Keep: `classify_ctx_kind` (used by transform.rs), `dollar_to_qrl_name` (used by transform.rs)
    8. Keep the `use crate::types::CtxKind;` import
    9. Re-add only the tests for `dollar_to_qrl_name` and `classify_ctx_kind` in the test module since those functions are retained

    **entry_strategy.rs:**
    1. Remove `#![allow(unused)]` on line 7
    2. Delete `should_extract` function (lines 17-19) -- only used in its own test, not in production code
    3. Delete `should_hoist` function (lines 22-30) -- only used in its own test, not in production code
    4. Delete `needs_separate_file` function (lines 34-36) -- only used in its own test, not in production code
    5. Keep `should_inline` -- used by transform.rs (lines 379, 456, 794, 954) and lib.rs (line 188)
    6. Update the test module to remove tests for the deleted functions, keep only `test_should_inline`

    **collector.rs (minor fix):**
    1. Remove unused import `use crate::words;` on line 19 -- collector.rs never references `words::` anything
    2. Prefix unused variable `ns` with underscore on line 834: change `JSXAttributeName::NamespacedName(ns)` to `JSXAttributeName::NamespacedName(_ns)`
  </action>
  <verify>
    Run `cargo build 2>&1 | grep -c "warning:"` -- the count should drop by at least 12 warnings compared to the current 22 (specifically: 2 from errors.rs allow+unused, 5 from words.rs, 3 from entry_strategy.rs, 1 from collector.rs words import, 1 from collector.rs ns variable). Run `cargo test` to confirm all 165 tests pass.
  </verify>
  <done>errors.rs has only create_source_error, words.rs has only classify_ctx_kind and dollar_to_qrl_name, entry_strategy.rs has only should_inline, collector.rs has no unused import or variable warnings. All 165 tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Delete unused functions and structs from code_move.rs, emit.rs, import_rewrite.rs, and props_destructuring.rs</name>
  <files>
    crates/qwik-optimizer-oxc/src/code_move.rs
    crates/qwik-optimizer-oxc/src/emit.rs
    crates/qwik-optimizer-oxc/src/import_rewrite.rs
    crates/qwik-optimizer-oxc/src/props_destructuring.rs
  </files>
  <action>
    **code_move.rs:**
    1. Delete `build_segment_code` function (lines 22-28) -- it's a wrapper around `build_segment_code_with_hoisted` that is never called. All callers use `build_segment_code_with_hoisted` directly.

    **emit.rs:**
    1. Remove the `minify` field from `EmitOptions` struct (line 14) -- never read by `emit_module`. The function does not use minification settings.
    2. Delete `normalize_code` function (lines 68-81) -- never called in production code
    3. Update the two test functions that construct `EmitOptions` to remove the `minify: MinifyMode::None` field (lines 102 and 145)
    4. After removing the `minify` field, check if `use crate::types::MinifyMode;` on line 9 is still needed. If not, remove it.

    **import_rewrite.rs:**
    1. Delete `ImportChanges` struct (lines 17-26) -- never constructed
    2. Delete `compute_import_changes` function (lines 29-67) -- never called in production code
    3. Delete the test module's tests for `compute_import_changes` (`test_compute_import_changes_basic` and `test_compute_import_changes_inline`) and the entire `#[cfg(test)] mod tests` block since those are the only tests
    4. After deleting, check if `use crate::types::ImportInfo;` on line 14 is still needed. If not, remove it.

    **props_destructuring.rs:**
    1. Delete `rewrite_array_elements` function (lines 459-493) -- never called
    2. Delete `rewrite_arguments` function (lines 498-end of function) -- never called
    3. Delete `array_element_as_expression_mut` helper (lines 435-447) -- only used by deleted rewrite_array_elements
    4. Delete `argument_as_expression_mut` helper (lines 450-454) -- only used by deleted rewrite_arguments
    5. Prefix unused variable `spread` with underscore on line 397: change `Argument::SpreadElement(spread)` to `Argument::SpreadElement(_spread)`
  </action>
  <verify>
    Run `cargo build 2>&1 | grep -c "warning:"` -- should show only the types.rs field warnings remaining (approximately 6-8 warnings about unread fields, which are addressed in plan 15-02). Run `cargo test` to confirm all 165 tests pass.
  </verify>
  <done>No unused function, struct, or variable warnings remain in code_move.rs, emit.rs, import_rewrite.rs, or props_destructuring.rs. All 165 tests pass.</done>
</task>

</tasks>

<verification>
1. `cargo build 2>&1 | grep "warning:" | grep -v "generated\|duplicates\|apply\|poc"` shows only types.rs field warnings (no function/constant/struct/variable warnings)
2. `cargo test` -- all 165 tests pass (158 unit + 7 spec)
3. `grep -r "#!\[allow(unused)\]" crates/qwik-optimizer-oxc/src/` returns no results
4. `grep "BUILDER_IO_QWIK" crates/qwik-optimizer-oxc/src/words.rs` returns no results
</verification>

<success_criteria>
- errors.rs contains only create_source_error, no #![allow(unused)]
- words.rs contains only classify_ctx_kind and dollar_to_qrl_name (no duplicate constants, no unused functions)
- entry_strategy.rs contains only should_inline, no #![allow(unused)]
- code_move.rs has no unused wrapper function
- emit.rs EmitOptions has no unused minify field, no unused normalize_code
- import_rewrite.rs has no unused ImportChanges struct or compute_import_changes function
- props_destructuring.rs has no unused rewrite_array_elements or rewrite_arguments
- All 165 tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/15-dead-code-removal/15-01-SUMMARY.md`
</output>
