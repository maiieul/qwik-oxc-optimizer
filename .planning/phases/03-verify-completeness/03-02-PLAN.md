---
phase: 03-verify-completeness
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - .planning/spec/example_dead_code.md
  - .planning/spec/example_dev_mode.md
  - .planning/spec/example_dev_mode_inlined.md
  - .planning/spec/example_noop_dev_mode.md
  - .planning/spec/example_prod_node.md
  - .planning/spec/example_server_auth.md
  - .planning/spec/example_build_server.md
  - .planning/spec/example_strip_server_code.md
  - .planning/spec/example_strip_client_code.md
  - .planning/tools/audit-specs.py
autonomous: true

must_haves:
  truths:
    - "All 6 files with zero AST details blocks now have at least an 'omitted for brevity' placeholder"
    - "All spec files with isServer/isBrowser/isDev replacement in output have CONV-10 documented"
    - "example_server_auth has segment metadata for all its ENTRY POINT modules"
    - "Final audit reports zero issues across both structural and convention completeness passes"
  artifacts:
    - path: ".planning/spec/example_dead_code.md"
      provides: "Spec file with AST details placeholder added"
      contains: "<details>"
    - path: ".planning/spec/example_server_auth.md"
      provides: "Spec file with segment metadata for all entry points"
      contains: "Segment Metadata"
    - path: ".planning/phases/03-verify-completeness/audit-report-final.txt"
      provides: "Final verification report showing zero issues"
  key_links:
    - from: ".planning/tools/audit-specs.py"
      to: ".planning/spec/*.md"
      via: "final re-audit confirms all issues resolved"
      pattern: "Total issues: 0"
---

<objective>
Fix all semantic issues: add missing AST details blocks, add missing CONV-10 conventions, add missing segment metadata, and verify final zero-issue audit across all 162 spec files.

Purpose: Plan 01 normalized cosmetic formatting. This plan addresses the substantive gaps: 6 files with no AST details blocks, CONV-10 detection gap (gen-spec.py had `pass` for it), 1 file with missing segment metadata. After this, all Phase 3 success criteria are met.

Output: Updated spec files with all semantic gaps filled, final clean audit report.
</objective>

<execution_context>
@/Users/jackshelton/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jackshelton/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-verify-completeness/03-RESEARCH.md
@.planning/phases/03-verify-completeness/03-01-SUMMARY.md
@.planning/tools/gen-spec.py
@.planning/tools/audit-specs.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add missing AST details blocks and fix segment metadata</name>
  <files>
    .planning/spec/example_dead_code.md
    .planning/spec/example_dev_mode.md
    .planning/spec/example_dev_mode_inlined.md
    .planning/spec/example_noop_dev_mode.md
    .planning/spec/example_prod_node.md
    .planning/spec/example_server_auth.md
  </files>
  <action>
Fix the 6 files that have zero `<details>` AST blocks. For each file:

1. After `### Source Code` code block, add an input AST details placeholder:
```
<details>
<summary>Input AST (OXC)</summary>

AST omitted for brevity. Parse with: `cat <<'EOF' | ./oxc-ast-util/target/release/oxc-ast-util tsx`

</details>
```

2. After each module's output code block (before `#### Segment Metadata` if present, or before the next `### Module:` heading), add an output AST details placeholder:
```
<details>
<summary>Output AST (OXC)</summary>

AST omitted for brevity.

</details>
```

3. For `example_server_auth.md` specifically: this file has 2 ENTRY POINT modules but 0 Segment Metadata sections. Read the snapshot file at `swc-optimizer/core/src/snapshots/qwik_core__test__example_server_auth.snap` to extract the segment metadata JSON for each entry point module, then add `#### Segment Metadata` sections with the extracted JSON after each entry point module's output code block (and after its new `<details>` AST block).

Use a targeted Python script or direct file editing. Do NOT re-run gen-spec.py (that would destroy Key behavior notes and other valuable content).

CRITICAL: Preserve all existing content in these files. Only ADD the missing details blocks and segment metadata. Do not modify existing convention descriptions, key behavior notes, or code blocks.
  </action>
  <verify>
1. `grep -rL '<details>' .planning/spec/*.md` returns empty (all 162 files now have at least one details block)
2. `grep -c '<details>' .planning/spec/example_server_auth.md` returns at least 3 (input + 2 output modules)
3. `grep 'Segment Metadata' .planning/spec/example_server_auth.md` returns matches for each entry point
4. Content spot-check: existing Key behavior notes and convention descriptions in these 6 files are preserved
  </verify>
  <done>All 6 files have AST details placeholders. example_server_auth has segment metadata for all entry point modules. No existing content was destroyed.</done>
</task>

<task type="auto">
  <name>Task 2: Fix CONV-10 gaps and run final zero-issue audit</name>
  <files>
    .planning/spec/example_build_server.md
    .planning/spec/example_prod_node.md
    .planning/spec/example_dev_mode.md
    .planning/spec/example_dev_mode_inlined.md
    .planning/spec/example_noop_dev_mode.md
    .planning/spec/example_strip_server_code.md
    .planning/spec/example_strip_client_code.md
    .planning/spec/example_server_auth.md
    .planning/tools/audit-specs.py
  </files>
  <action>
**Step 1: Identify CONV-10 files.**

CONV-10 (Const Replacement) applies when `isServer`, `isBrowser`, or `isDev` identifiers in the INPUT code are replaced with literal `true` or `false` in the OUTPUT code. Check the following candidate files by reading both their input and output code sections:

- `example_build_server` -- has `isServer` in input?
- `example_prod_node` -- has `isServer`/`isBrowser`/`isDev` in input?
- `example_dev_mode` -- has `isDev` in input?
- `example_dev_mode_inlined` -- same
- `example_noop_dev_mode` -- same
- `example_strip_server_code` -- has `isServer` in input?
- `example_strip_client_code` -- has `isServer`/`isBrowser` in input?
- `example_server_auth` -- has `isServer` in input?

For each file where the input contains `isServer`/`isBrowser`/`isDev` AND the output replaces it with a literal boolean: if CONV-10 is NOT already documented in `## Conventions Applied`, ADD it as a new bullet. Use this format:

```
- **[CONV-10] Const Replacement**: `isServer` replaced with literal `true`/`false` based on build configuration
```

Adjust the specific identifiers mentioned based on what is actually replaced in each file. Append the new CONV-10 line after existing conventions (before any `**Key behavior:**` notes). Do NOT modify existing convention descriptions.

Some of these files may already document CONV-10 (they were manually generated in Plan 01). Only add where missing.

**Step 2: Fix any remaining convention completeness issues.**

Run `python3 .planning/tools/audit-specs.py` and check Pass 2 output. If any other convention completeness issues are reported beyond CONV-10 (unlikely but possible), fix those too using the same approach: add the missing convention bullet without modifying existing descriptions.

**Step 3: Update audit script if needed.**

If the audit script's CONV-10 detection needs refinement (e.g., it currently reports false positives or misses cases), update the heuristic in audit-specs.py to match the actual CONV-10 behavior: compare input code's `isServer`/`isBrowser`/`isDev` usage against output code to determine if replacement occurred.

**Step 4: Run final audit.**

Run `python3 .planning/tools/audit-specs.py` one final time. Save output to `.planning/phases/03-verify-completeness/audit-report-final.txt`. The report MUST show zero issues across both Pass 1 (structural) and Pass 2 (convention completeness).

If any issues remain, fix them iteratively until the audit is clean.
  </action>
  <verify>
1. `python3 .planning/tools/audit-specs.py` exits with code 0 and reports "Total issues: 0" (or equivalent clean output)
2. `grep -c 'CONV-10' .planning/spec/example_strip_server_code.md` returns at least 1
3. `ls .planning/spec/*.md | wc -l` returns 162 (no files created or deleted)
4. `cat .planning/phases/03-verify-completeness/audit-report-final.txt` confirms clean audit
  </verify>
  <done>All convention completeness issues resolved. Final audit reports zero issues across all 162 files for both structural consistency and convention completeness. Phase 3 success criteria fully met.</done>
</task>

</tasks>

<verification>
Phase 3 success criteria verification:
1. `ls .planning/spec/*.md | wc -l` == 162 (exactly 162 spec files exist)
2. `python3 .planning/tools/audit-specs.py` reports zero issues on both passes:
   - Pass 1: All files have required sections in order, consistent formatting
   - Pass 2: No missing conventions detected in any file
3. `grep -rL '<details>' .planning/spec/*.md` returns empty (all files have AST details)
4. All 65 files with Key behavior notes still contain them (grep count matches pre-fix count)
</verification>

<success_criteria>
- All 6 files with missing details blocks now have AST placeholders
- example_server_auth has segment metadata for all entry point modules
- CONV-10 is documented in every file where const replacement occurs
- Final audit reports zero issues: 0 structural, 0 convention completeness
- 162 spec files remain (no files created or deleted)
- Key behavior notes preserved in all 65 files
</success_criteria>

<output>
After completion, create `.planning/phases/03-verify-completeness/03-02-SUMMARY.md`
</output>
