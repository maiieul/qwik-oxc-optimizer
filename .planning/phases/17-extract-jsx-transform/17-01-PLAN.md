---
phase: 17-extract-jsx-transform
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/qwik-optimizer-oxc/src/jsx_transform.rs
  - crates/qwik-optimizer-oxc/src/transform.rs
  - crates/qwik-optimizer-oxc/src/lib.rs
autonomous: true

must_haves:
  truths:
    - "All JSX transformation free functions live in jsx_transform.rs, not transform.rs"
    - "transform.rs delegates to jsx_transform.rs for JSX element/fragment/children transformation"
    - "All 165 tests pass identically -- zero behavior change"
  artifacts:
    - path: "crates/qwik-optimizer-oxc/src/jsx_transform.rs"
      provides: "All JSX transformation free functions extracted from transform.rs"
      min_lines: 1300
    - path: "crates/qwik-optimizer-oxc/src/transform.rs"
      provides: "Core QwikTransform with JSX delegation, no JSX logic duplication"
    - path: "crates/qwik-optimizer-oxc/src/lib.rs"
      provides: "Module declaration for jsx_transform"
      contains: "mod jsx_transform"
  key_links:
    - from: "crates/qwik-optimizer-oxc/src/transform.rs"
      to: "crates/qwik-optimizer-oxc/src/jsx_transform.rs"
      via: "crate::jsx_transform:: function calls"
      pattern: "jsx_transform::(transform_jsx_element_inner|transform_jsx_fragment_inner)"
---

<objective>
Extract all JSX transformation free functions from transform.rs into a new jsx_transform.rs module.

Purpose: transform.rs is 2,745 lines. ~1,450 lines are JSX-specific free functions that don't access QwikTransform state. Extracting them makes transform.rs focused on the Traverse implementation and segment recording, while jsx_transform.rs owns all JSX-to-function-call conversion logic.

Output: jsx_transform.rs module with ~1,450 lines, transform.rs reduced to ~1,300 lines, all 165 tests passing.
</objective>

<execution_context>
@/Users/jackshelton/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jackshelton/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@crates/qwik-optimizer-oxc/src/transform.rs
@crates/qwik-optimizer-oxc/src/lib.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create jsx_transform.rs with extracted free functions</name>
  <files>crates/qwik-optimizer-oxc/src/jsx_transform.rs, crates/qwik-optimizer-oxc/src/lib.rs</files>
  <action>
Create `crates/qwik-optimizer-oxc/src/jsx_transform.rs` containing ALL of the following free functions currently in transform.rs (lines 1164-2622). These are ALL module-level functions below the `impl Traverse` block and above `minify_fn_string`:

**Functions to extract (in order as they appear):**
1. `normalize_jsx_text` (line 1170) -- pub(crate)
2. `transform_event_attr_name` (line 1205) -- pub(crate)
3. `is_const_jsx_value` (line 1246) -- pub(crate)
4. `SignalWrapResult` enum (line 1251) -- pub(crate)
5. `detect_signal_wrap` (line 1273) -- pub(crate)
6. `is_call_on_value` (line 1314) -- pub(crate)
7. `ReactiveDep` struct (line 1325) -- pub(crate)
8. `contains_function_call` (line 1333) -- pub(crate)
9. `collect_reactive_deps` (line 1373) -- pub(crate)
10. `collect_reactive_deps_inner` (line 1394) -- private (called only by collect_reactive_deps)
11. `get_root_identifier` (line 1595) -- pub(crate)
12. `has_chain_depth` + inner `depth` (line 1605) -- private (called only by collect_reactive_deps_inner)
13. `is_imported_identifier` (line 1617) -- private (called only by collect_reactive_deps_inner)
14. `build_fn_signal_wrapping` (line 1627) -- pub(crate)
15. `replace_identifier_in_code` (line 1714) -- private (called only by build_fn_signal_wrapping)
16. `is_ident_char` (line 1740) -- private (called only by replace_identifier_in_code and minify_expression_string)
17. `minify_expression_string` (line 1745) -- pub(crate) (also used in transform.rs tests)
18. `escape_string_literal` (line 1790) -- pub(crate)
19. `extract_identifier_name` (line 1795) -- pub(crate)
20. `build_bind_event_handler` (line 1810) -- pub(crate)
21. `build_tag_expression` (line 1849) -- pub(crate)
22. `build_jsx_member_expr` (line 1879) -- pub(crate)
23. `jsx_expression_to_expression` (line 1901) -- pub(crate)
24. `jsx_attr_value_to_expression` (line 1960) -- pub(crate)
25. `transform_jsx_element_inner` (line 1996) -- pub(crate)
26. `transform_jsx_fragment_inner` (line 2438) -- pub(crate)
27. `transform_jsx_children` (line 2498) -- pub(crate)

Also extract the standalone helper that sits just below `impl QwikTransform` (before the Traverse impl):
28. `get_jsx_lambda_span` (line 597) -- pub(crate)
29. `transform_attr_name_for_display` (line 606) -- pub(crate)

**Module header for jsx_transform.rs:**
```rust
//! JSX transformation: convert JSX elements/fragments into _jsxSorted/_jsxSplit function calls.
//!
//! This module contains all the pure JSX transformation logic extracted from transform.rs.
//! Functions here operate on AST nodes and ImportTracker without needing QwikTransform state.
```

**Imports needed in jsx_transform.rs:**
- `use oxc::ast::ast::*;`
- `use oxc::span::SPAN;`
- `use oxc_traverse::TraverseCtx;`
- `use crate::import_rewrite;`
- `use crate::transform::ImportTracker;` (ImportTracker stays in transform.rs since QwikTransform owns it)

Functions that are only called within jsx_transform.rs itself should remain private (no pub(crate)). Functions called from transform.rs need pub(crate).

Add `mod jsx_transform;` to lib.rs, placed alphabetically after `mod import_rewrite;` and before `mod is_const;`.

Do NOT move:
- `minify_fn_string` (line 2626) -- used for sync$ serialization, not JSX-specific
- `argument_to_expression` (line 2656) -- pub(crate) used elsewhere, not JSX-specific
- The `#[cfg(test)] mod tests` block -- stays in transform.rs (tests that reference moved functions will use `crate::jsx_transform::`)
- Any `impl QwikTransform` methods (lines 137-589) -- these use `self` and stay in transform.rs
- Any `impl Traverse` methods -- these stay in transform.rs
  </action>
  <verify>
`cargo check -p qwik-optimizer-oxc 2>&1` -- should show no errors (may show warnings, that's fine at this stage)
Confirm jsx_transform.rs exists and has >1300 lines: `wc -l crates/qwik-optimizer-oxc/src/jsx_transform.rs`
  </verify>
  <done>jsx_transform.rs exists with all 29 listed functions, lib.rs declares the module, cargo check passes</done>
</task>

<task type="auto">
  <name>Task 2: Update transform.rs delegation and verify all tests pass</name>
  <files>crates/qwik-optimizer-oxc/src/transform.rs</files>
  <action>
Remove the extracted functions from transform.rs (lines 1164-2622 and lines 597-616). Replace with imports from jsx_transform.

**Update call sites in transform.rs to use qualified paths:**

1. In `create_jsx_event_segments_recursive` (around line 517-529):
   - `transform_attr_name_for_display(...)` -> `crate::jsx_transform::transform_attr_name_for_display(...)`

2. In `create_jsx_event_segments_recursive` (around line 517):
   - `get_jsx_lambda_span(...)` -> `crate::jsx_transform::get_jsx_lambda_span(...)`

3. In `exit_expression` JSX element/fragment handling (around line 735):
   - `transform_jsx_element_inner(...)` -> `crate::jsx_transform::transform_jsx_element_inner(...)`
   - `transform_jsx_fragment_inner(...)` -> `crate::jsx_transform::transform_jsx_fragment_inner(...)`

4. In the sync$ handling in exit_expression (around line 788):
   - `minify_fn_string(...)` stays -- this function is NOT moved

5. In the `#[cfg(test)] mod tests` block:
   - `minify_expression_string(...)` -> `crate::jsx_transform::minify_expression_string(...)`

**Alternative to qualified paths:** Add a `use crate::jsx_transform::{...}` import block at the top of transform.rs for the functions called from transform.rs. This is cleaner. The needed imports are:
```rust
use crate::jsx_transform::{
    get_jsx_lambda_span, transform_attr_name_for_display,
    transform_jsx_element_inner, transform_jsx_fragment_inner,
};
```
And in the test module: `use crate::jsx_transform::minify_expression_string;`

**Also verify:** `ImportTracker` must remain `pub(crate)` in transform.rs since jsx_transform.rs imports it via `crate::transform::ImportTracker`.

After removal, transform.rs should be ~1,250-1,350 lines. Run `wc -l` to confirm significant reduction.

Run `cargo fmt -p qwik-optimizer-oxc` after all changes.
  </action>
  <verify>
`cargo test -p qwik-optimizer-oxc 2>&1` -- all 165 tests pass
`cargo clippy -p qwik-optimizer-oxc 2>&1` -- no errors (warnings acceptable)
`wc -l crates/qwik-optimizer-oxc/src/transform.rs` -- should be ~1,250-1,350 lines (down from 2,745)
`wc -l crates/qwik-optimizer-oxc/src/jsx_transform.rs` -- should be ~1,400-1,500 lines
  </verify>
  <done>
transform.rs reduced to ~1,300 lines, delegates to jsx_transform for all JSX logic, all 165 tests pass, no clippy errors, cargo fmt clean
  </done>
</task>

</tasks>

<verification>
1. `cargo test -p qwik-optimizer-oxc` -- all 165 tests pass (zero regressions)
2. `cargo clippy -p qwik-optimizer-oxc` -- no errors
3. `wc -l crates/qwik-optimizer-oxc/src/transform.rs` confirms significant line reduction (~1,300 lines)
4. `wc -l crates/qwik-optimizer-oxc/src/jsx_transform.rs` confirms extraction (~1,450 lines)
5. `grep -c "fn " crates/qwik-optimizer-oxc/src/jsx_transform.rs` shows ~29 functions extracted
6. No JSX transformation logic duplicated between transform.rs and jsx_transform.rs
</verification>

<success_criteria>
- jsx_transform.rs exists with all JSX transformation free functions (~1,450 lines, 29 functions)
- transform.rs is ~1,300 lines (down from 2,745) and delegates to jsx_transform for JSX handling
- lib.rs declares `mod jsx_transform`
- All 165 tests pass identically -- pure refactor, zero behavior change
- No clippy errors, cargo fmt clean
</success_criteria>

<output>
After completion, create `.planning/phases/17-extract-jsx-transform/17-01-SUMMARY.md`
</output>
