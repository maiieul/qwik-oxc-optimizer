---
phase: 24-runtime-bug-fixes
plan: 06
type: execute
wave: 6
depends_on: ["24-05"]
files_modified:
  - crates/qwik-optimizer-oxc/tests/output_audit.rs
  - .planning/phases/24-runtime-bug-fixes/FINAL-AUDIT.md
autonomous: false
gap_closure: true

must_haves:
  truths:
    - "Re-running the output audit shows 4 or fewer runtime-breaking deviations (only truly-missing-module from example_qwik_react and relative_paths)"
    - "The 22 missing-import-used deviations from the previous audit are resolved"
    - "All 162 specs produce output without optimizer errors"
    - "FINAL-AUDIT.md reflects the updated deviation counts"
    - "A regression test asserts runtime-breaking deviation count never exceeds the threshold, preventing reintroduction of fixed deviations"
  artifacts:
    - path: ".planning/phases/24-runtime-bug-fixes/FINAL-AUDIT.md"
      provides: "Updated final audit report with reduced runtime-breaking count"
      contains: "truly-missing-module"
    - path: "crates/qwik-optimizer-oxc/tests/output_audit.rs"
      provides: "Audit test with regression threshold assertion that fails if runtime-breaking deviations exceed 4"
      contains: "runtime_breaking_count"
  key_links:
    - from: "crates/qwik-optimizer-oxc/tests/output_audit.rs"
      to: "all 162 spec files"
      via: "spec_parser loading and transform_modules execution"
      pattern: "load_all_specs|transform_modules"
    - from: "crates/qwik-optimizer-oxc/tests/output_audit.rs"
      to: "regression threshold assertion"
      via: "missing-import-used classification and count check"
      pattern: "runtime_breaking_count|assert!"
---

<objective>
Re-run the full output audit to validate the gap closure, add regression threshold assertions, update FINAL-AUDIT.md, and get user verification.

Purpose: Plan 05 fixed the capture analysis to handle module-level declarations. This plan validates that the 22 missing-import-used deviations are resolved, adds regression assertions to prevent reintroduction of those deviations (FIX-03), and updates the audit report. The target is 4 or fewer runtime-breaking deviations (only the truly-missing-module issues from example_qwik_react and relative_paths, which are accepted limitations).

Output: Updated FINAL-AUDIT.md confirming the gap closure. Regression test in output_audit.rs. User verification that Phase 24 is complete.
</objective>

<execution_context>
@/Users/jackshelton/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jackshelton/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-runtime-bug-fixes/FINAL-AUDIT.md
@.planning/phases/24-runtime-bug-fixes/24-05-SUMMARY.md
@crates/qwik-optimizer-oxc/tests/output_audit.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-run output audit and update FINAL-AUDIT.md</name>
  <files>
    crates/qwik-optimizer-oxc/tests/output_audit.rs
    .planning/phases/24-runtime-bug-fixes/FINAL-AUDIT.md
  </files>
  <action>
Run the complete output audit to measure the impact of the capture analysis fix:

```
cargo test --package qwik-optimizer-oxc --test output_audit -- --nocapture 2>&1
```

Parse the audit-raw.json output and classify remaining deviations. The expected outcome:
- missing-import-used deviations: 22 -> 0 (or near-zero)
- truly-missing-module deviations: 4 (unchanged -- example_qwik_react and relative_paths)
- Total runtime-breaking: 26 -> 4 (or near 4)

If any missing-import-used deviations remain, investigate each one:
1. Read the specific spec's expected output vs actual output
2. Determine if it's a different root cause than module_level_decls
3. Fix if possible within scope, or document as a known limitation

Update FINAL-AUDIT.md with:
- Updated Executive Summary table (Before/After columns updated)
- Updated Runtime-Breaking Deviations section
- Remove or update the "Missing Import Details" section (should be resolved)
- Keep the "Truly Missing Modules" section (unchanged)
- Add a "Gap Closure" section documenting what Plan 05 fixed
- Updated Conclusion with final deviation counts

The key metric: runtime-breaking deviations should go from 26 to 4 (or fewer). The Phase 24 success criteria says "zero runtime-breaking deviations" but the truly-missing-module ones are accepted limitations per STATE.md blockers.
  </action>
  <verify>
`cargo test --package qwik-optimizer-oxc --test output_audit` completes successfully.

FINAL-AUDIT.md exists and shows 4 or fewer runtime-breaking deviations.

`cargo test --package qwik-optimizer-oxc` passes (all tests).
  </verify>
  <done>
Output audit re-run confirms missing-import-used deviations resolved. FINAL-AUDIT.md updated with final counts. Only truly-missing-module deviations remain (accepted limitation).
  </done>
</task>

<task type="auto">
  <name>Task 2: Add regression threshold assertion to output_audit.rs (FIX-03)</name>
  <files>
    crates/qwik-optimizer-oxc/tests/output_audit.rs
  </files>
  <action>
Add runtime-breaking deviation classification and a regression threshold assertion to the `output_audit` test in `output_audit.rs`. This implements requirement FIX-03: "Test harness updated to validate fixed deviations don't regress."

Currently the test (line 545) has `// DO NOT assert on deviations -- this is an audit, not a pass/fail test.` This must change. The audit should still produce its full JSON output, but it must NOW also assert that the runtime-breaking deviation count stays at or below a threshold.

**What to add (after the JSON write, before or replacing the existing final assert):**

1. **Classify deviations as runtime-breaking vs cosmetic.** After the main audit loop completes (after line 490), iterate through `all_deviations` and count "runtime-breaking" ones. A deviation is runtime-breaking if:
   - `match_type` is `"unmatched_expected"` (truly-missing-module), OR
   - `diff_summary` contains `"missing imports"` AND the missing import is actually used in the body code (missing-import-used pattern)

   The simplest reliable classification: count deviations where `match_type == "unmatched_expected"` (these are the truly-missing-module ones). For code deviations, check if `diff_summary` contains `"missing imports"` and then verify the missing import symbols appear in the `actual_code_normalized` body (not just in import lines). This is the "missing-import-used" pattern.

   A simpler alternative that's still effective: classify a deviation as runtime-breaking if:
   - `match_type == "unmatched_expected"` (module completely missing), OR
   - The diff_summary mentions `"missing imports"` AND the expected code has `useLexicalScope` (capture-dependent code missing its imports)

   Choose whichever approach is more straightforward after seeing the actual deviation data from Task 1.

2. **Add the threshold assertion.** After counting runtime-breaking deviations:

```rust
// Regression gate (FIX-03): runtime-breaking deviations must not exceed threshold.
// After Plan 24-05 fixed module-level declaration captures, only 4 truly-missing-module
// deviations remain (example_qwik_react: 2, relative_paths: 2). These are accepted
// limitations requiring multi-file compilation or pre-compiled QRL support.
// If this assertion fails, a code change has reintroduced runtime-breaking deviations.
const RUNTIME_BREAKING_THRESHOLD: usize = 4;
assert!(
    runtime_breaking_count <= RUNTIME_BREAKING_THRESHOLD,
    "Regression detected: {} runtime-breaking deviations exceeds threshold of {}. \
     Previously fixed deviations may have been reintroduced. \
     Check audit-raw.json for details.",
    runtime_breaking_count,
    RUNTIME_BREAKING_THRESHOLD
);
```

3. **Also add the count to the printed summary** (in the eprintln block around line 530):

```rust
eprintln!("Runtime-breaking deviations: {} (threshold: {})", runtime_breaking_count, RUNTIME_BREAKING_THRESHOLD);
```

4. **Remove or update the comment on line 545** that says "DO NOT assert on deviations". Replace with a comment explaining the regression gate.

5. **Run the full test suite** to confirm the assertion passes:
```
cargo test --package qwik-optimizer-oxc --test output_audit -- --nocapture 2>&1
```

The threshold of 4 is chosen to match exactly the known accepted limitations (4 truly-missing-module deviations). If Plan 05's fix works correctly, the count should be exactly 4. Any future code change that breaks capture analysis will push this above 4 and fail the test.
  </action>
  <verify>
`cargo test --package qwik-optimizer-oxc --test output_audit` passes with the new assertion.

Grep output_audit.rs for `RUNTIME_BREAKING_THRESHOLD` to confirm the assertion exists.

Grep output_audit.rs for `runtime_breaking_count` to confirm classification logic exists.

Verify the old "DO NOT assert" comment is removed or updated.
  </verify>
  <done>
output_audit.rs now classifies deviations as runtime-breaking vs cosmetic and asserts runtime_breaking_count <= 4. If any future change reintroduces the 22 missing-import-used deviations (or any other runtime-breaking regression), `cargo test` will fail. FIX-03 requirement satisfied.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: User verification of gap closure results</name>
  <files>
    .planning/phases/24-runtime-bug-fixes/FINAL-AUDIT.md
  </files>
  <action>
Present the updated FINAL-AUDIT.md to the user. Plan 05 fixed the capture analysis to handle module-level declarations in nested segment bodies. This plan re-ran the full audit to confirm the 22 missing-import-used deviations are resolved, and added a regression threshold assertion (FIX-03) to prevent reintroduction.
  </action>
  <verify>
User reviews the following:
1. Read `.planning/phases/24-runtime-bug-fixes/FINAL-AUDIT.md` -- confirm runtime-breaking deviations reduced from 26 to 4 (or fewer)
2. Run `cargo test --package qwik-optimizer-oxc` -- confirm all tests pass (including the new regression assertion)
3. Run `cargo test --package qwik-optimizer-oxc --test output_audit -- --nocapture` -- review audit summary, confirm "Runtime-breaking deviations: 4 (threshold: 4)" line
4. Verify the remaining 4 deviations are only from example_qwik_react and relative_paths (accepted limitations)
5. (Optional) Spot-check a previously-failing spec like example_exports by reading its expected vs actual segment output

Type "approved" if the gap closure is confirmed, or describe any issues found.
  </verify>
  <done>
User confirms runtime-breaking deviations reduced from 26 to 4 (or fewer). Regression assertion in place. Phase 24 gap closure approved.
  </done>
</task>

</tasks>

<verification>
- FINAL-AUDIT.md updated with gap closure results
- Runtime-breaking deviations: 26 -> 4 (only truly-missing-module)
- output_audit.rs has RUNTIME_BREAKING_THRESHOLD assertion that fails if count exceeds 4
- `cargo test --package qwik-optimizer-oxc` passes (all tests including regression gate)
- User has reviewed and approved
</verification>

<success_criteria>
- Runtime-breaking deviations reduced from 26 to 4 (93 -> 99% reduction from Phase 23 baseline of 293)
- All 22 missing-import-used deviations resolved
- Only truly-missing-module deviations remain (accepted limitation)
- Regression threshold assertion prevents reintroduction of fixed deviations (FIX-03)
- User approves the gap closure
</success_criteria>

<output>
After completion, create `.planning/phases/24-runtime-bug-fixes/24-06-SUMMARY.md`
</output>
</output>
