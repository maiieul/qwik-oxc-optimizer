---
phase: 24-runtime-bug-fixes
plan: 04
type: execute
wave: 4
depends_on: ["24-03"]
files_modified:
  - crates/qwik-optimizer-oxc/tests/output_audit.rs
  - .planning/phases/24-runtime-bug-fixes/FINAL-AUDIT.md
autonomous: false

must_haves:
  truths:
    - "Re-running the full output audit shows zero runtime-breaking deviations"
    - "All 162 specs produce semantically correct output modules"
    - "A regression test exists that will catch re-introduction of any fixed deviation"
  artifacts:
    - path: "crates/qwik-optimizer-oxc/tests/output_audit.rs"
      provides: "Updated audit test that can serve as regression check"
    - path: ".planning/phases/24-runtime-bug-fixes/FINAL-AUDIT.md"
      provides: "Final audit report confirming zero runtime-breaking deviations"
  key_links:
    - from: "crates/qwik-optimizer-oxc/tests/output_audit.rs"
      to: "all 162 spec files"
      via: "spec_parser loading and transform_modules execution"
      pattern: "load_all_specs|transform_modules"
---

<objective>
Validate all runtime-breaking deviations are fixed and establish regression prevention.

Purpose: This is the final validation plan for Phase 24. After Plans 01-03 fixed QRL extraction, import resolution, capture analysis, and codegen issues, this plan runs the complete audit to confirm zero runtime-breaking deviations remain, then converts the audit into a regression test that prevents re-introduction of fixed issues.

Output: FINAL-AUDIT.md confirming zero runtime-breaking deviations. Updated output_audit.rs that can function as a regression test.
</objective>

<execution_context>
@/Users/jackshelton/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jackshelton/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-output-audit/AUDIT-REPORT.md
@.planning/phases/24-runtime-bug-fixes/24-01-SUMMARY.md
@.planning/phases/24-runtime-bug-fixes/24-02-SUMMARY.md
@.planning/phases/24-runtime-bug-fixes/24-03-SUMMARY.md
@crates/qwik-optimizer-oxc/tests/output_audit.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run final audit and fix any remaining runtime-breaking deviations</name>
  <files>
    crates/qwik-optimizer-oxc/tests/output_audit.rs
    crates/qwik-optimizer-oxc/src/transform.rs
    crates/qwik-optimizer-oxc/src/collector.rs
    crates/qwik-optimizer-oxc/src/code_move.rs
    .planning/phases/24-runtime-bug-fixes/FINAL-AUDIT.md
  </files>
  <action>
Run the complete output audit to identify any remaining runtime-breaking deviations:

```
cargo test --package qwik-optimizer-oxc --test output_audit -- --nocapture 2>&1
```

Parse the results and classify remaining deviations using the same methodology as Phase 23:
- Runtime-breaking: missing modules, empty segments, wrong captures, missing imports, QRL extraction failures
- Cosmetic: extra modules, extra imports, hash differences, naming conventions

**If zero runtime-breaking deviations remain:** Write FINAL-AUDIT.md confirming this.

**If runtime-breaking deviations remain:** Fix them. These will be tail-end edge cases not addressed by the systematic fixes in Plans 01-03. For each remaining deviation:
1. Read the specific spec's expected output
2. Compare with actual output
3. Identify the specific code path that produces the wrong result
4. Fix it

Common tail-end issues:
- Edge cases in TypeScript type annotation handling during body serialization
- Async arrow functions not handled by find_arrow_position in code_move.rs
- Generator functions ($() wrapping generators)
- Computed property names in destructuring affecting capture analysis
- Multiple $-calls in the same expression (ternary with two QRLs)

Write FINAL-AUDIT.md with:
- Final deviation counts (runtime-breaking should be 0)
- Cosmetic deviation summary (acceptable per locked decision: "Fix only runtime-breaking deviations")
- List of specs now passing (all modules match expected)
- List of specs with only cosmetic deviations
  </action>
  <verify>
Run `cargo test --package qwik-optimizer-oxc --test output_audit` -- completes without assertion failures.

FINAL-AUDIT.md exists and shows zero runtime-breaking deviations.

`cargo test --package qwik-optimizer-oxc` -- all existing tests pass.
  </verify>
  <done>
Zero runtime-breaking deviations across all 162 specs. FINAL-AUDIT.md documents the final state. All cosmetic deviations are cataloged but explicitly deferred per project decision.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User verification of final audit results</name>
  <files>
    .planning/phases/24-runtime-bug-fixes/FINAL-AUDIT.md
  </files>
  <action>
Present the FINAL-AUDIT.md results to the user for review. All runtime-breaking deviations from the Phase 23 audit have been fixed across Plans 01-04. The OXC optimizer now produces semantically correct output for every spec.
  </action>
  <verify>
User reviews the following:
1. Read `.planning/phases/24-runtime-bug-fixes/FINAL-AUDIT.md` -- confirm zero runtime-breaking deviations
2. Run `cargo test --package qwik-optimizer-oxc` -- confirm all tests pass
3. Run `cargo test --package qwik-optimizer-oxc --test output_audit -- --nocapture` -- confirm audit completes successfully
4. Review the cosmetic deviation summary -- confirm these are acceptable (extra modules, hash differences, naming conventions)
5. Spot-check 2-3 previously-failing specs by reading their expected vs actual output

Type "approved" if zero runtime-breaking deviations confirmed, or describe any issues found.
  </verify>
  <done>
User confirms zero runtime-breaking deviations and approves the Phase 24 results. Phase 24 is complete.
  </done>
</task>

</tasks>

<verification>
- FINAL-AUDIT.md exists with zero runtime-breaking deviations
- `cargo test --package qwik-optimizer-oxc` passes (all tests)
- `cargo test --package qwik-optimizer-oxc --test output_audit` passes
- User has reviewed and approved the final audit
</verification>

<success_criteria>
- Runtime-breaking deviations: 293 -> 0
- All 162 specs produce semantically correct output
- Existing test suite passes without regressions
- User approves the final audit results
</success_criteria>

<output>
After completion, create `.planning/phases/24-runtime-bug-fixes/24-04-SUMMARY.md`
</output>
