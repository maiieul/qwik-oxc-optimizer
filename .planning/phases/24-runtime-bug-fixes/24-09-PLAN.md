---
phase: 24-runtime-bug-fixes
plan: 09
type: execute
wave: 8
depends_on: ["24-07", "24-08"]
files_modified:
  - crates/qwik-optimizer-oxc/tests/output_audit.rs
  - .planning/phases/24-runtime-bug-fixes/FINAL-AUDIT.md
autonomous: false
gap_closure: true

must_haves:
  truths:
    - "Re-running the output audit shows 4 or fewer runtime-breaking deviations (only truly-missing-module from example_qwik_react and relative_paths)"
    - "The 6 missing-import-used deviations from Plan 06 are resolved (or reduced to edge cases)"
    - "RUNTIME_BREAKING_THRESHOLD in output_audit.rs is lowered to match actual count"
    - "FINAL-AUDIT.md reflects the final deviation counts"
    - "User has verified and approved the results"
  artifacts:
    - path: ".planning/phases/24-runtime-bug-fixes/FINAL-AUDIT.md"
      provides: "Final audit report with gap closure results showing minimal runtime-breaking deviations"
    - path: "crates/qwik-optimizer-oxc/tests/output_audit.rs"
      provides: "Updated regression threshold matching actual runtime-breaking count"
      contains: "RUNTIME_BREAKING_THRESHOLD"
  key_links:
    - from: "crates/qwik-optimizer-oxc/tests/output_audit.rs"
      to: "all 162 spec files"
      via: "spec_parser loading and transform_modules execution"
      pattern: "load_all_specs|transform_modules"
---

<objective>
Re-run the full output audit after Plans 07-08, lower the regression threshold, update FINAL-AUDIT.md, and get user verification.

Purpose: Plans 07 and 08 fixed declaration collection edge cases and JSX import source propagation. This plan validates the fixes reduced the 6 missing-import-used deviations, lowers the RUNTIME_BREAKING_THRESHOLD assertion to the new floor, and gets final user sign-off on Phase 24.

Output: Updated FINAL-AUDIT.md and output_audit.rs with final counts. User verification that Phase 24 is truly complete.
</objective>

<execution_context>
@/Users/jackshelton/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jackshelton/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-runtime-bug-fixes/FINAL-AUDIT.md
@.planning/phases/24-runtime-bug-fixes/24-07-SUMMARY.md
@.planning/phases/24-runtime-bug-fixes/24-08-SUMMARY.md
@crates/qwik-optimizer-oxc/tests/output_audit.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-run audit, lower threshold, update FINAL-AUDIT.md</name>
  <files>
    crates/qwik-optimizer-oxc/tests/output_audit.rs
    .planning/phases/24-runtime-bug-fixes/FINAL-AUDIT.md
  </files>
  <action>
Run the complete output audit:

```
cargo test --package qwik-optimizer-oxc --test output_audit -- --nocapture 2>&1
```

Parse results and count remaining runtime-breaking deviations. Expected outcome:
- missing-import-used: 6 → 0 (or near-zero)
- truly-missing-module: 4 (unchanged)
- Total runtime-breaking: 10 → 4 (or near-4)

If any missing-import-used deviations remain, investigate each one and document.

**Update RUNTIME_BREAKING_THRESHOLD** in output_audit.rs to match the actual count:
```rust
const RUNTIME_BREAKING_THRESHOLD: usize = 4; // or whatever the actual count is
```

**Update FINAL-AUDIT.md** with:
- Add a "Gap Closure Round 2" section documenting Plans 07-08 impact
- Update the Executive Summary table with a new column for Plan 09 (final)
- Update the Conclusion with the true final deviation counts
- Update the remaining deviations list (should only be truly-missing-module)

Run the full test suite to confirm the lowered threshold passes:
```
cargo test --package qwik-optimizer-oxc
```
  </action>
  <verify>
`cargo test --package qwik-optimizer-oxc --test output_audit` passes with the lowered threshold.

`cargo test --package qwik-optimizer-oxc` passes (all tests).

FINAL-AUDIT.md shows 4 or fewer runtime-breaking deviations.
  </verify>
  <done>
Audit confirms missing-import-used deviations resolved. Threshold lowered. FINAL-AUDIT.md updated with true final counts.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User verification of final Phase 24 results</name>
  <files>
    .planning/phases/24-runtime-bug-fixes/FINAL-AUDIT.md
  </files>
  <action>
Present the final FINAL-AUDIT.md to the user. Phase 24 has been through two rounds of gap closure:
- Round 1 (Plans 05-06): Module-level declaration self-imports → 293→10
- Round 2 (Plans 07-08): Pattern defaults, TS enums, default exports, JSX import source → 10→4 (target)

The remaining deviations should only be the 4 truly-missing-module ones (architectural limitations).
  </action>
  <verify>
User reviews:
1. Read `.planning/phases/24-runtime-bug-fixes/FINAL-AUDIT.md` — confirm final counts
2. Run `cargo test --package qwik-optimizer-oxc` — all tests pass
3. Run `cargo test --package qwik-optimizer-oxc --test output_audit -- --nocapture` — confirm regression gate
4. Verify remaining deviations are only architectural limitations (example_qwik_react, relative_paths)

Type "approved" if Phase 24 is complete, or describe any remaining issues.
  </verify>
  <done>
User confirms Phase 24 complete. Only architectural limitations remain. Ready for Phase 25 (NAPI Crate).
  </done>
</task>

</tasks>

<verification>
- FINAL-AUDIT.md updated with final gap closure results
- Runtime-breaking deviations at 4 or fewer (only truly-missing-module)
- RUNTIME_BREAKING_THRESHOLD lowered in output_audit.rs
- `cargo test --package qwik-optimizer-oxc` passes
- User has reviewed and approved
</verification>

<success_criteria>
- Runtime-breaking deviations: 10 → 4 (only truly-missing-module architectural limitations)
- All 6 missing-import-used deviations resolved across Plans 05-08
- Regression threshold at true floor (4)
- Phase 24 approved by user
</success_criteria>

<output>
After completion, create `.planning/phases/24-runtime-bug-fixes/24-09-SUMMARY.md`
</output>
</content>
</invoke>