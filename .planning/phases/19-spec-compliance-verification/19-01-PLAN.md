---
phase: 19-spec-compliance-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/qwik-optimizer-oxc/tests/spec_tests.rs
autonomous: true

must_haves:
  truths:
    - "All 154 unit tests pass with zero failures"
    - "All 7 spec tests pass with zero failures"
    - "Module count match is 157/162 or better (no regressions from v3.0 baseline)"
    - "All 250 metadata assertions pass (ctxName, ctxKind, captures)"
    - "Zero transform errors across all 162 specs"
    - "No new test failures compared to v3.0 baseline"
  artifacts:
    - path: "crates/qwik-optimizer-oxc/tests/spec_tests.rs"
      provides: "Updated spec validation test with v4.0 regression assertions"
      contains: "v4.0"
  key_links:
    - from: "crates/qwik-optimizer-oxc/tests/spec_tests.rs"
      to: "crates/qwik-optimizer-oxc/src/lib.rs"
      via: "transform_modules public API"
      pattern: "transform_modules"
---

<objective>
Run full spec compliance verification and regression comparison against v3.0 baseline, documenting that all v4.0 refactoring preserved spec compliance.

Purpose: This is the final gate for the v4.0 Code Quality Refactor milestone. It confirms that phases 14-18 (style cleanup, dead code removal, targeted fixes, JSX extract, const_replace rewrite) introduced zero regressions to the optimizer's behavioral correctness.

Output: Updated spec_tests.rs with v4.0 regression guard test, full compliance report in SUMMARY.
</objective>

<execution_context>
@/Users/jackshelton/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jackshelton/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@crates/qwik-optimizer-oxc/tests/spec_tests.rs
@.planning/phases/13-source-maps-full-validation/13-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and capture v4.0 compliance baseline</name>
  <files>crates/qwik-optimizer-oxc/tests/spec_tests.rs</files>
  <action>
Run the complete test suite and capture results for comparison against v3.0 baseline:

1. Run `cargo test --package qwik-optimizer-oxc --lib -- --nocapture 2>&1` and capture unit test count (expect 154 pass, 0 fail).

2. Run `cargo test --package qwik-optimizer-oxc test_full_spec_validation -- --nocapture 2>&1` and capture:
   - Module count match (expect 157/162)
   - Metadata match (expect 250/250)
   - Known deviations (expect 5 module count, 16 capture, 3 diagnostic)

3. Run `cargo test --package qwik-optimizer-oxc test_all_specs_coverage_report -- --nocapture 2>&1` and capture:
   - Transform OK count (expect 162/162)
   - Transform Error count (expect 0)

4. Compare against v3.0 baseline from phase 13-04 SUMMARY:
   - v3.0: 157/162 module count, 250/250 metadata, 158 unit tests + 7 spec tests
   - v4.0: Should be same or better

5. Add a regression guard test `test_v4_regression_guard` to spec_tests.rs that hard-asserts the v4.0 compliance numbers:
   - assert module_count_match >= 157
   - assert metadata_match == 250
   - assert transform_err == 0
   - assert unit test count >= 154

The regression guard test should be a simple, fast test that runs the full spec validation inline and panics if any metric regresses below the v4.0 baseline. Structure it as a wrapper around the existing test_full_spec_validation logic but with explicit numeric assertions on the summary counters.

Do NOT modify any optimizer source code. This is a read-only verification phase. The only file change is adding the regression guard test to spec_tests.rs.
  </action>
  <verify>
Run `cargo test --package qwik-optimizer-oxc 2>&1` and confirm:
- All unit tests pass (154+)
- All spec tests pass (7+ including new regression guard)
- Zero failures
  </verify>
  <done>
- `cargo test --package qwik-optimizer-oxc` reports 0 failures
- test_v4_regression_guard exists and passes
- Module count match is 157/162 or better
- Metadata match is 250/250
- Transform errors are 0/162
- Unit test count is 154+
  </done>
</task>

<task type="auto">
  <name>Task 2: Document v4.0 compliance report in SUMMARY</name>
  <files>No source files modified -- SUMMARY only</files>
  <action>
Create the phase SUMMARY documenting the full compliance report. Include:

1. **v3.0 vs v4.0 comparison table:**
   | Metric | v3.0 Baseline | v4.0 Final | Delta |
   |--------|--------------|------------|-------|
   | Module count match | 157/162 | ???/162 | ??? |
   | Metadata assertions | 250/250 | ???/250 | ??? |
   | Transform errors | 0/162 | ???/162 | ??? |
   | Unit tests | 158 | ???  | ??? |
   | Spec tests | 7 | ??? | ??? |
   | Known deviations (module) | 5 | ??? | ??? |
   | Known deviations (capture) | 16 | ??? | ??? |
   | Known deviations (diagnostic) | 3 | ??? | ??? |

   Fill in actual values from Task 1 output.

2. **Deviation inventory** (unchanged from v3.0):
   - 5 module count deviations: example_3, example_component_with_event_listeners_inside_loop, example_immutable_analysis, example_qwik_react, relative_paths
   - 16 capture analysis deviations (13 JSX event handler, 3 false positive)
   - 3 diagnostic deviations

3. **v4.0 refactoring impact assessment:**
   - Phase 14 (style cleanup): zero impact
   - Phase 15 (dead code removal): zero impact
   - Phase 16 (targeted fixes): zero impact (minify bug fix was additive improvement)
   - Phase 17 (JSX extract): zero impact (pure refactor)
   - Phase 18 (const_replace VisitMut): zero impact (pure refactor)

4. **Conclusion:** v4.0 milestone requirement SPEC-01 satisfied.

The SUMMARY goes to `.planning/phases/19-spec-compliance-verification/19-01-SUMMARY.md` using the standard summary template.
  </action>
  <verify>
File exists at `.planning/phases/19-spec-compliance-verification/19-01-SUMMARY.md` and contains the comparison table and deviation inventory.
  </verify>
  <done>
- SUMMARY.md created with full v3.0 vs v4.0 comparison
- All metrics documented with actual values
- SPEC-01 requirement status clearly stated
- Phase 19 marked complete
  </done>
</task>

</tasks>

<verification>
1. `cargo test --package qwik-optimizer-oxc` passes with 0 failures
2. Module count match >= 157/162 (SPEC-01 requirement)
3. Metadata assertions = 250/250
4. No new test failures vs v3.0 baseline (158 unit + 7 spec)
5. test_v4_regression_guard test exists and passes
</verification>

<success_criteria>
- SPEC-01 requirement satisfied: 157/162 or better module count match
- All 250 metadata assertions pass
- Zero transform errors
- No regressions from v3.0 baseline
- Regression guard test prevents future regressions
- Compliance report documents the final state
</success_criteria>

<output>
After completion, create `.planning/phases/19-spec-compliance-verification/19-01-SUMMARY.md`
</output>
